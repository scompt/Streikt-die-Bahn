application: streiktdiebahn
version: 1
runtime: python
api_version: 1

handlers:
- url: /static
  static_dir: static

- url: .*
  script: main.py

- url: /manager/.*
  script: main.py
  login: required
  auth_fail_action: unauthorized